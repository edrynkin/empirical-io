import numpy as np

def generate_data(theta_1, cost_func, RC, theta_3, T, beta):

    x = np.zeros(T)
    i = np.zeros(T)
    epsilon = np.random.gumbel(0.0, 1.0, (2, T))

    return epsilon

def transition(x, i, theta_3)

    mileage = np.random.binomial(1, theta_3)

    return (1 - i)*(x + mileage)

def

print generate_data(0, 0, 0, 0, 10, 0)